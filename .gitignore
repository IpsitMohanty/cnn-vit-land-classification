# Python virtual environment
.venv/
__pycache__/
*.pyc

# Local tooling config
.claude/

# Trained model weights (large; regenerate by rerunning the notebooks)
*.keras
*.pth
# demo/cnn_state_dict.pth is INTENTIONALLY tracked despite the *.pth rule above:
# the demo has to run standalone on a fresh clone / HF Space, and at 78MB it's
# well under GitHub's 100MB limit. This is not the ignore rule failing.
!demo/cnn_state_dict.pth

# Downloaded dataset / sample data (regenerated by the notebooks' download cells)
images_dataSAT
new-Policies.txt
markdown-sample.md
facebook-chat.json
mlb-teams-2012.csv
file-sample.docx

# Jupyter
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db
